# 英文單字學習網 製作方法

> 線上版：<https://anitatsai13.github.io/english-vocab-site/>
> 單一自包含 HTML（資料＋程式＋樣式全內嵌）＋PWA 三件套（manifest / sw.js / icon.svg），無框架、無 build step。

## 檔案結構

```
english-vocab-site/
├── index.html            ← App 本體（約 1MB；所有資料與程式）
├── manifest.webmanifest  ← PWA 安裝資訊
├── sw.js                 ← Service Worker 離線快取（改版要升快取版號）
└── icon.svg              ← 圖示
```

另有同步副本 `英文單字學習網/英文單字學習網.html`（與 index.html 逐位元相同，每次改版用檔案雜湊比對確認同步）。

## 資料層（index.html 內的巨型常數）

| 常數 | 內容 | 形狀重點 |
|---|---|---|
| `VOCAB` | 1400+ 單字，**一行一筆 JSON** | `{en,pos,zh,lesson,group,tip,rel,ex:{en,zh},def,exam:{src,q,opts,ans},clz:[{src,q,opts,ans},…]}` |
| `GROUPS` | 課程分組（kang 各冊、EASON 子群、easy3…） | EASON 父群組含子列切換 |
| `POLY` | 一字多義 | `{g,les,en,senses:[{p,zh,ex,t}],exam:[…]}` |
| `VERB_ALL` | 動詞三態 | |
| `PHRASE` | 片語會話（重要片語／生活常用語／慣用語／生活美語／片語我最通） | 片語我最通依動詞四個一組（`set` 欄可跨動詞分組） |
| `PEXAM` | 片語選擇題 | `{src,q,opts,ans,exp}` |
| `GEXAM` | （已移除）文法考古題全數遷至文法學習網 | |

- `exam`＝真實考古題（考古題分頁）；`clz`＝**陣列**，講義「看例句選字」真題，一字可掛多組，考古題也複製一份進 clz。
- **資料注入務必檢查稀疏洞**：`for(i) if(!(i in VOCAB))`——雙逗號會產生 hole，`map/filter` 驗不出來。

## 測驗引擎

- `makeQuestion(word,pool,type)`：拼字／聽拼／中英選擇／看例句選字（cloze）等題型。
- **cloze 分支**：字有 `clz` 且非 EASON 群 → 隨機抽一組真題、用原句原選項（答案可為變化形，經 `ansText` 比對）；否則例句挖空＋誘答。
- **誘答策略**：EASON／Wonderskills 用 `halfPool`（限同半冊、拼字相似、10 選項）；kang 各冊 4 選項跨課抽。
- 片語練習混入 PEXAM 選擇題；一字多義測驗含「講義選擇題」分支（POLY.exam）。
- 題數選單（#phNum/#pexNum：10/20/30/50/全部）。

## 錯題本與雲端

- 錯題存 localStorage，支援 題型×冊別課程×課別 三維篩選重測（wSel/wSelG/wSelL）。
- **文法錯題轉運**：本站不留文法錯題，經 outbox → Supabase `grammar_inbox` 自動搬到文法學習網。
- Supabase 會員跨裝置同步錯題（anon key 內嵌、RLS 保護）。

## 其他機制

- Web Speech API 唸例句（cloze 空格唸成 blank 不洩答案；語速可放慢）。
- 全站搜尋框；學習卡翻卡；單字表可勾選單字出題。
- 回歸測試：`cloze-test.js`（22 項斷言：語法、選項數、誘答半冊限制、不洩題）每次改版必跑。

## 部署與維護流程

1. 改 `index.html` → 跑回歸測試 → 同步副本（hash 比對）
2. `git commit`（中文訊息＋AI 協助範圍）→ `git push` → GitHub Pages 自動部署
3. 用「唯一內容標記」輪詢線上版確認上線（20 秒 × 18 次）
4. 手機看到舊版＝PWA 快取，重新整理兩次或升 sw 版號
